import styles from './footer.module.css'

export const Footer = () => {
    return (
        <footer className={styles.footer}>
            <h3>Desenvolvido por Carolinne Victória :)</h3>
        </footer>
    )
}